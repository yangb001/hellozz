以下是**最终完整详细架构设计文档**，整合了所有讨论，并明确回应了您的每一个疑问。适用于本地 Python 实现，无 Docker、无外部队列，使用嵌入式向量库（LanceDB/Chroma）作为长期记忆，SQLite 存储会话。

---

# 多会话 Agent 框架 — 最终架构设计（本地实现）

## 1. 核心疑问汇总解答

在设计之前，先将历史讨论中您关注的所有关键问题给出明确答案：

| 疑问                                  | 结论                                                         |
| ------------------------------------- | ------------------------------------------------------------ |
| 每个会话的上下文由谁管理？            | **会话管理器**创建、持有、持久化 `SessionContext`。运行时无状态，执行时借用，结束交还。 |
| 记忆系统与会话管理器的关系？          | 会话管理器为每个会话绑定一个实现 `BaseMemory` 的记忆实例；记忆实例内部通过 `session_id` 隔离数据。记忆系统负责存储/检索算法，不感知会话生命周期。 |
| 记忆触发机制？                        | 由记忆系统内部策略控制：每条消息实时追加短期缓冲；智能策略会调用轻量 LLM 判断消息重要性并即时提取；也可配置每 N 轮/会话关闭触发全量提取。运行时完全不知情。 |
| 长期记忆如何提取？用什么模型？        | 记忆系统通过框架的 LLM 网关调用专用轻量模型（如本地 Ollama 的 `llama3`）进行事实抽取，异步执行，不阻塞主对话。模型可配置。 |
| 组件是否必须同代码仓？可插拔吗？      | 通过接口完全解耦，不必同仓。可任意替换实现，例如记忆、规划器、工具、LLM 提供商。 |
| 微服务拆分后用什么协议通信？          | 内部服务间推荐 gRPC（流/一元）和消息队列；WebSocket 仅用于用户与接入层。 |
| 群组会话和交叉记忆的实现？            | 会话上下文增加 `participants` 及消息的 `sender_id`；记忆系统增加用户级记忆分区，检索时传入发言人 ID 融合个人记忆，隐私基于参与者列表控制。 |
| 接入层设计？                          | 独立 FastAPI 应用，提供 REST 管理接口和 WebSocket 流式对话接口，通过会话管理器接口处理业务。 |
| 为何最终选择向量库而不是 SQLite FTS？ | 需要语义理解同义表达；嵌入式向量库（LanceDB/Chroma）已足够轻量，无外部依赖，能提供关键词无法做到的模糊匹配。框架架构支持平滑切换，先用 SQLite 也可后续升级。 |

---

## 2. 架构设计原则

- **分层解耦**：每一层仅依赖下一层的抽象接口，实现可替换。
- **会话隔离**：使用 Actor 模型（`asyncio.Queue`）保证单会话内消息顺序处理，多会话并行。
- **无状态运行时**：Agent 运行时从会话管理器租借上下文，不持有状态。
- **流式事件驱动**：所有执行过程以事件流形式产出，支持实时推送。
- **本地零依赖**：无 Docker、无外部队列，使用 SQLite 和嵌入式向量库，所有数据落盘本地文件。

---

## 3. 系统分层架构

```
┌───────────────────────────────────────────────────────┐
│                   接入层 (Gateway)                     │
│   REST API / WebSocket  │ 认证/路由/事件转换          │
└──────────────────────────┬────────────────────────────┘
                           │ 调用 SessionManager 接口
┌──────────────────────────▼────────────────────────────┐
│               会话管理器 (Session Manager)             │
│  会话生命周期 · Actor并发隔离 · 上下文持久化(SQLite)   │
│  事件总线 (EventBus)                                   │
└──┬──────────────┬──────────────┬──────────────────────┘
   │              │              │
   ▼              ▼              ▼
┌──────┐  ┌──────────────┐  ┌─────────────────┐
│Agent │  │  记忆系统     │  │  规划器 & 工具   │
│ 运行时│  │  MemoryMgr   │  │  ReAct / Graph  │
│      │  │  - 短期缓冲   │  │  工具注册中心   │
│      │  │  - 向量记忆   │  │                 │
│      │  │  - 提取器     │  │                 │
└──────┘  └──────────────┘  └─────────────────┘
                           │
┌──────────────────────────▼────────────────────────────┐
│               基础设施 (Infrastructure)                 │
│  模型网关 (LLM Gateway) │ 存储: SQLite(会话) + 向量库  │
└───────────────────────────────────────────────────────┘
```

所有模块通过 `interfaces` 包定义抽象协议，运行时依赖在启动时由配置组装。

---

## 4. 核心模块详细设计

### 4.1 接口层 (interfaces)

提供所有数据模型和抽象基类，零依赖。

**事件模型**：
```python
class Event(BaseModel):
    type: str       # thought, action, observation, text_token, final_answer, error
    content: str
    metadata: dict = {}
    timestamp: datetime = datetime.utcnow()
```

**会话上下文**：
```python
class SessionContext(BaseModel):
    session_id: str
    session_type: str = "private"
    participants: List[str] = []
    status: str = "active"
    messages: List[Message] = []
    agent_state: Dict = {}      # 规划器执行栈快照
    tool_instances: Dict = {}
    created_at / last_active ...
```

**消息**：含 `sender_id` 支持多用户。

**抽象接口**：
- `BaseMemory`: `save`, `retrieve`, `extract_long_term`, `clear`
- `BasePlanner`: `plan_and_act(ctx, memory, tools, llm_call) -> AsyncIterator[Event]`
- `BaseTool`: `name`, `description`, `run(input, session_id)`

### 4.2 会话管理器 (core/session_manager.py)

**职责**：会话生命周期、Actor 并发隔离、持久化。

- 使用 SQLite 存储会话全部数据（表结构见后）。
- 内存中维护活跃会话的 `SessionContext` 副本和 Actor 队列。
- **Actor 模型**：每个会话一个 `asyncio.Queue`，后台任务 `_session_actor(sid)` 从队列取消息，串行调用 Agent 运行时，输出事件列表。
- **持久化时机**：每次 Actor 处理完一条消息后，将上下文写回 SQLite。
- **崩溃恢复**：启动时扫描 SQLite 中 `status=active` 的会话，重新构建队列并重启 Actor（如果仍需要处理未完成消息）。

**关键方法**：
- `create_session(user_id, session_type, participants)` → 新建上下文，入库，启动 Actor
- `process_message(session_id, user_msg)` → 将消息投入对应 Actor 队列，返回 `Future[List[Event]]`
- `get_session(sid)` → 从内存或数据库加载

### 4.3 Agent 运行时 (runtime/agent_runtime.py)

无状态，核心方法：
```python
async def run(ctx, user_input, memory, tools, planner, llm_gateway) -> AsyncIterator[Event]:
    # 1. 添加用户消息到 ctx.messages
    # 2. 保存到记忆系统
    # 3. 构建 llm_call 闭包（调用 LLM 网关）
    # 4. 调用 planner.plan_and_act，逐事件产出
    # 5. 将 assistant 最终回复添加至 ctx 和记忆
```

### 4.4 记忆系统 (memory/)

**架构**：统一门面 `MemoryManager` 实现 `BaseMemory`，内部组合：
- `BufferMemory`：短期缓冲，内存列表，每个会话保留最近 N 轮。
- `VectorMemory`：长期记忆，使用嵌入式向量库（LanceDB/Chroma），数据存于 `./data/vectors`。
- `MemoryExtractor`：通过 LLM 抽取事实，异步写入向量库。

**触发策略**：
- 每条消息都实时追加到短期缓冲。
- 若配置 `trigger: smart`，每条消息会异步通过 `MemoryExtractor.is_important` 判断是否需要即时提取；若重要，则调用 `extract_long_term`。
- 若配置 `trigger: every_n:5`，则每 5 轮对话后批量提取。
- 会话关闭时强制提取。

**检索融合**：
```python
async def retrieve(session_id, query, user_ids=None, top_k=5):
    short = await buffer.get_recent(session_id, 10)
    long = await vector.query(session_id, query, top_k)
    user_long = ""
    if user_ids:
        for uid in user_ids:
            user_long += await vector.query_user(uid, query, top_k=2)
    return f"{user_long}\n{long}\nRecent: {short}"
```

**向量库实现**（以 LanceDB 为例）：
```python
class VectorMemory:
    def __init__(self, db_path, embedding_func):
        self.db = lancedb.connect(db_path)
        self.embed = embedding_func

    async def add(self, session_id, text, metadata=None):
        vec = self.embed(text)
        # 写入表 session_{session_id}
        ...

    async def query(self, session_id, query, top_k=5):
        query_vec = self.embed(query)
        # 在表中检索，返回文本拼接
        ...
    # 用户记忆使用 user_{user_id} 表
```

### 4.5 规划器 (planners/)

- `ReActPlanner`：标准思考-行动-观察循环，每一步通过 LLM 决定动作，执行工具，观察结果，直至给出最终回答。
- 可替换为 `PlanExecutePlanner` 或 `GraphPlanner`。

### 4.6 接入层 (gateway/)

- FastAPI 应用，提供 REST 接口和 WebSocket 端点。
- 认证中间件：JWT 或 API Key。
- WebSocket 路径 `/ws/chat?session_id=xxx`，连接后循环接收用户消息，调用 `session_manager.process_message`，接收事件列表，转换为 JSON 推送。
- REST：`POST /sessions`、`GET /sessions/{id}`、`DELETE /sessions/{id}` 等。

### 4.7 基础设施 (infrastructure/)

- **LLM 网关**：封装不同模型提供商（本地 Ollama、远端 API），统一 `generate` 和 `stream` 方法。
- **SQLite 存储**：使用 `aiosqlite`，封装会话 CRUD。
- **嵌入模型**：使用 `sentence-transformers`，本地运行，无需 API 调用。

---

## 5. 群组会话与交叉记忆的具体实现

- **群组会话创建**：`session_type="group"`，`participants` 含多个用户 ID。
- **多用户消息处理**：客户端 WebSocket 消息中必须携带 `sender_id`，存储到 `Message` 对象。
- **记忆写入**：提取事实时，若事实绑定某个用户（LLM 判断），事实被打上 `user_id` 标签，同时存入 `user_{user_id}` 表。
- **记忆检索**：运行时在收到群组消息时，获取 `sender_id`，调用 `memory.retrieve` 时传入 `user_ids=[sender_id]`，记忆系统自动融合该用户的历史个人记忆。
- **隐私**：检索用户记忆前，验证 `sender_id` 是否在当前会话的 `participants` 列表中，否则拒绝。

---

## 6. 配置与依赖注入

```yaml
sqlite:
  path: "./data/sessions.db"

memory:
  short_term_size: 20
  vector_db: "lancedb"          # 或 "chroma"
  vector_path: "./data/vectors"
  embedding_model: "all-MiniLM-L6-v2"
  extraction:
    trigger: "smart"            # "every_n:5" 或 "on_close"
    model: "ollama/llama3"      # 用于提取的模型

llm:
  default: "ollama/llama3"
  providers:
    ollama:
      base_url: "http://localhost:11434"

planner: "planners.react_planner.ReActPlanner"
```

启动时，`gateway/main.py` 读取配置，实例化所有模块并注入 `SessionManager`。

---

## 7. 从单体到微服务的演化

当前所有组件进程内调用。未来拆分时，只需：

1. 为需要远程化的接口编写 gRPC 服务定义。
2. 实现接口的 gRPC 客户端适配器（如 `GrpcMemoryClient` 实现 `BaseMemory`）。
3. 在配置中将 `memory.vector_db` 改为 `grpc://memory-service:50051`，其余代码**零改动**。

---

## 8. 总结

本设计提供了一套**完整的、可直接编码的本地多会话 Agent 框架**，包含了会话管理、记忆系统、规划器、接入层的所有关键实现细节，并回答了您关于上下文归属、记忆触发、向量库选型、交叉记忆等所有疑问。所有组件均基于抽象接口，开箱即用，且预留了向分布式系统演进的清晰路径。